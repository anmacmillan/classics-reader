# Catullus *Carmina* Implementation Plan

1. Extract and pin Perseus Catullus Latin/English poems 1–116 where transmitted (including 14a).
2. Add the import manifest and run the existing Whitaker Latin dictionary generator.
3. Validate all 116 poem units, run `make import && make check`, bump cache versions, push, and verify Pages.
